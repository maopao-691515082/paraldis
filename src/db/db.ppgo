import (
    "db/okv/zkv";
    "db/okv";
)

final var kDBCount int = 16;

var dbs []okv.DB;

func init_db()
{
    dbs.resize(kDBCount);
    for (var i : 0 .. kDBCount)
    {
        dbs[i] = zkv.open();
    }
}

func get_db(vslot_id int) okv.DB
{
    return dbs[vslot_id % kDBCount];
}
