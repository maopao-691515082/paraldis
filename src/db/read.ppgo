import (
    "../proto";
)

public interface CmdReadProc
{
    public func do_read(client proto.Client, vs VSlot);
}

public func do_read(client proto.Client, crp CmdReadProc, hash_tag string)
{
    crp.do_read(client, VSlot(hash_tag_to_vslot_id(hash_tag), read_only = true));
}
